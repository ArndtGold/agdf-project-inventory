# Management View Structure

Select and order existing report-index statement IDs. Copy each complete rendered statement byte for
byte, reference the source report digest and list omitted detail. The management view is a projection,
not a summary. Do not paraphrase, merge statements, introduce a finding, add an assessment or create
a second recommendation.
